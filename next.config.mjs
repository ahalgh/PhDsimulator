/** @type {import('next').NextConfig} */
const nextConfig = {
    // Removed 'output: export' to enable API routes for server-side data fetching
    reactStrictMode: true,
};

export default nextConfig;
